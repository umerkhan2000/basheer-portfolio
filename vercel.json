{
  "version": 2,
  "builds": [
    { "src": "vite.config.*", "use": "@vercel/static-build" }
  ],
  "framework": null,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
